# NYC Indoor Pool Finder — https://pools.thinkdesign.com/
#
# The site now lives at the root of its own subdomain (Netlify), so THIS file is
# the effective robots.txt and is honoured by crawlers. Before the move it was
# served at thinkdesign.com/pools/robots.txt and ignored, and the real one was a
# hand-edited static file at the WordPress web root on WP Engine.
#
# That WP Engine robots.txt still carries a second Sitemap line pointing at
# https://thinkdesign.com/pools/sitemap.xml (added 2026-09-01). Drop it once the
# /pools/ 301 to this subdomain is in place — it is no longer this repo's
# concern, and it cannot be deployed from here.

User-agent: *
Allow: /

Sitemap: https://pools.thinkdesign.com/sitemap.xml
